{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Modules
    //--------------------------------------------------

    "rootDir": "./src",

    // Emit
    //--------------------------------------------------

    "emitDeclarationOnly": true,
    "outDir": "./dist"
  }
}
